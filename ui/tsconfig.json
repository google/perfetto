{
  "extends": "./tsconfig.base.json",
  "include": [ "src/**/*" ],
  "exclude": [
    "./node_modules/",
    "./src/test/diff_viewer/",
    "./src/service_worker/",
    "./src/gen/",
    "./out"
  ],
  "compilerOptions": {
    "outDir": "./out/tsc",
    // mediabunny pulls in @types/dom-webcodecs, whose global WebCodecs types
    // clash with TypeScript's built-in lib.dom; skip .d.ts checks to avoid it.
    "skipLibCheck": true,
    "types": ["*"],
    "lib": [
      "dom",                               // Need to be explicitly mentioned now since we're overriding default included libs.
      "es2024", // Need this to use Promise.allSettled, replaceAll, etc
      "dom.iterable",                      // For using for..of loops on NodeListOf<Element>
    ],
    "paths": {
      "*" : ["./*", "./node_modules/@tsundoku/micromodal_types/*"]
    },
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
  }
}
