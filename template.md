---
title: Title of the post, sentence case, no trailing period
author: @YourGithubHandle
summary: One or two sentences. This is the index card, the meta description and the feed entry, so make it the reason to click rather than a restatement of the title.
---

Open with the concrete thing: the symptom, the number, the surprise. Not a
preamble about how important performance is.

## A heading

Body. Reference images by plain filename, they get rewritten:

![What this shows](screenshot.png)

Link to docs and other posts with absolute paths, never `../`:

[the ftrace docs](/docs/data-sources/ftrace)

NOTE: callouts work here, as do mermaid diagrams, `<?tabs>` blocks and
highlighted code fences.

## Close with something the reader can run

A trace to open, a repo to clone, a query to paste.
