#!/bin/bash

# Path to your Python script
PYTHON_SCRIPT="tools/run_presubmit"

# Run the Python script
python3 "$PYTHON_SCRIPT"

# Check the exit code of the Python script
RESULT=$?
if [ $RESULT -ne 0 ]; then
    echo "Push failed: Python script returned error code $RESULT"
    exit 1
fi

exit 0
